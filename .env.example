# Base RPC Configuration
RPC_URL=https://mainnet.base.org
RPC_TIMEOUT=30

# Hydrex Contract Addresses
VOTER_ADDRESS=0xc69E3eF39E3fFBcE2A1c570f8d3ADF76909ef17b
HYDREX_ROUTER_ADDRESS=0x6f4bE24d7dC93b6ffcBAb3Fd0747c5817Cea3F9e
HYDREX_SWAP_DEPLOYER_ADDRESS=0x0000000000000000000000000000000000000000
HYDREX_SWAP_SKIP_TOKENS=

# Swap Execution Mode: "direct" (per-token exactInputSingle) or "router-batch" (single executeSwaps tx via routing API)
HYDREX_SWAP_EXECUTION_MODE=direct

# Multi-router batch mode settings (used when HYDREX_SWAP_EXECUTION_MODE=router-batch)
HYDREX_MULTI_ROUTER_ADDRESS=0x599bFa1039C9e22603F15642B711D56BE62071f4
HYDREX_ROUTING_API_URL=https://router.api.hydrex.fi
HYDREX_ROUTING_SOURCE=KYBERSWAP
HYDREX_ROUTING_SLIPPAGE_BPS=50
HYDREX_ROUTING_ORIGIN=hydrex-optimiser
DELEGATED_INFLIGHT_MAX_RETRIES=6
DELEGATED_INFLIGHT_RETRY_SECONDS=6
PENDING_NONCE_WAIT_SECONDS=90
PENDING_NONCE_POLL_SECONDS=2
# VoterV5 contract address on Base

# Your Configuration
MY_ESCROW_ADDRESS=0x0000000000000000000000000000000000000000
YOUR_VOTING_POWER=1000000
# Voting power in the same units as contract

# API Keys (Optional)
COINGECKO_API_KEY=
# Free tier works, but pro key recommended for better rate limits
# NOTE: a keyless CoinGecko client can only price 1 address per call; the
# bribe fetchers batch properly only when this key is set.

# Price sanity / routing-mispricing controls
PRICE_SANITY_MAX_SPIKE_RATIO=3.0
PRICE_SANITY_LOOKBACK_SECONDS=604800
# Comma-separated token addresses whose price must come from CoinGecko, never
# from the Hydrex routing quote. Thin-liquidity tokens can quote persistently
# high on the router; because the spike guard falls back to comparing against
# the *previous routing price*, a stable overprice never trips it.
# Audit routing vs cg_ref periodically and add any token whose median ratio
# stays above ~1.25 (2026-08-07 audit: REGENT 3.13x, BETR 1.89x).
HYDREX_ROUTING_COINGECKO_FALLBACK_TOKENS=
# Tokens to skip entirely when quoting via the router (unroutable / dust).
HYDREX_ROUTING_DEFER_TOKENS=

# Database
DATABASE_PATH=data/db/data.db

# Logging
LOG_LEVEL=INFO
LOG_FILE=hydrex_optimizer.log

# Cache Settings
PRICE_CACHE_TTL=300
# Cache token prices for 5 minutes

# Optimization Settings
MIN_VOTE_ALLOCATION=1000
# Minimum votes to allocate to any single gauge
MAX_GAUGES_TO_VOTE=10
# Maximum number of gauges to split votes across

# Auto top-k sweep settings (used by auto_voter / boundary_monitor)
AUTO_TOP_K_ENABLED=true
AUTO_TOP_K_MAX=50
AUTO_TOP_K_CANDIDATE_POOLS=60
AUTO_TOP_K_RETURN_TOLERANCE_PCT=2.0
AUTO_VOTE_RESOLVE_POOL_NAMES=false
AUTO_VOTE_TRIGGER_BLOCKS_BEFORE=20
AUTO_VOTE_SECOND_TRIGGER_BLOCKS_BEFORE=5
AUTO_VOTE_MAX_GAS_PRICE_GWEI=10
AUTO_VOTE_SECOND_MAX_GAS_PRICE_GWEI=20

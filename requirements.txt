# Direct runtime dependencies for hydrex-optimiser
# Python 3.9+
# Install: pip install -r requirements.txt

# Web3 / blockchain
web3==6.15.1
eth-account==0.11.3
eth-utils==5.3.1
eth-retry==0.3.5
multicall==0.12.3

# HTTP / API
requests==2.31.0

# Data
pandas==2.2.0
numpy==1.26.3
scipy==1.12.0
# src/database.py imports this directly. It was absent until 2026-09-24, so a fresh
# `pip install -r requirements.txt` could not import src.database and 10 tests errored.
# Nothing else pulls it in transitively, so it must be pinned here.
sqlalchemy==2.0.25

# Config / env
python-dotenv==1.0.1

# CLI / output
rich==13.7.0
tqdm==4.66.1

# Dev / test (not needed in production)
pytest==7.4.4
black==24.1.1
mypy==1.8.0

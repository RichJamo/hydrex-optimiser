# Revisions that changed formatting only, and should be skipped by git blame.
#
# Enable locally (once, per clone):
#     git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# GitHub's blame view reads this file automatically and needs no setup.
# Only add a revision here if it provably changed no behaviour.

# style(repo): apply black to every tracked python file
# 154 of 171 files, ~15k lines. Verified by AST equivalence across all 171 files:
# 0 semantic differences; the 21 raw differences were docstring trailing whitespace.
aa86296b45bfa192f2adc83454aafd07caecf855

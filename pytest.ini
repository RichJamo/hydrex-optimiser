[pytest]
# web3 ships a broken `pytest_ethereum` plugin (incompatible eth_typing) that
# fails to import and would abort collection. We don't use it — disable it so
# the suite runs without needing PYTEST_DISABLE_PLUGIN_AUTOLOAD=1.
addopts = -p no:pytest_ethereum
testpaths = tests
